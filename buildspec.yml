version: 0.2

phases:
  install:
    commands:
      - npm install
  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
  base-directory: dist

AWSTemplateFormatVersion: "2010-09-09"

Resources:
  DummyBucket:
    Type: AWS::S3::Bucket
